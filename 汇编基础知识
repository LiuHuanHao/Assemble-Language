同时修改CS IP的内容
jmp 2AE3:3

仅修改Ip的内容
jmp ax (类似于mov IP,ax)

CS存放指令的段地址，IP存放指令的偏移地址

20位地址总线传送能力为1M      2^10=1024
内部为16位结构，只能传送64k=(2^6)*(2^10)

段地址*16+偏移地址=物理地址

R命令查看、改变CPU寄存器的内容
D命令查看内存中的内容
E命令改写内存中的内容
U命令将内存中的机器命令翻译成汇编指令
T命令执行一条机器指令
A命令以汇编指令的格式在内存中写入一条机器指令

r ip 可以更改IP的值   r Cs 可以改变CS的值
可以用 a 指定位置了来写入汇编语句 
FFFF:05开始是存储CPU生产日期的地址
b8100是显存的地址

读取10000单元内容可以用如下程序进行
mov bx,10000H
mov ds,bx        ds用存放段寄存器的内容，[0]说明偏移地址为零
mov al,[0]      由于是al,所以只存放一个字节

8086CPU不支持数据直接送入段寄存器的操作，ds是一个段寄存器
mov ds,10000H   this is wrong!!!!

将数据从寄存器送入内存单元
mov bx,10000H
mov ds,bx
mov [0],al     al中八位数据送到10000:0处，ax则为16位



























